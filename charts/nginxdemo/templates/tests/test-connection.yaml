apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginxdemo.fullname" . }}-test-connection"
  labels:
    {{- include "nginxdemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginxdemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
